interface:
  display_name: "Graphify Codebase"
  short_description: "Map dependencies and stranded-code leads"
  default_prompt: "Use $graphify-codebase to trace dependencies, inspect orphan or widow leads, and verify every result in source."
